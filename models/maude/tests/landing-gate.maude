load ../landing-gate.maude
search [1] in LANDING-GATE : { request(promote,main,good,true,true) commit(promote,main,good) current(old,old,old,old,old,old,old,true) } =>* { admitted(promote,main,good) CFG:Cfg } .
search [1] in LANDING-GATE : { request(transport,main,good,true,true) commit(transport,main,good) current(old,old,old,old,old,old,old,true) } =>* { admitted(transport,main,good) CFG:Cfg } .
search [1] in LANDING-GATE : { request(undo,main,repaired,true,true) commit(undo,main,repaired) current(old,old,old,old,old,old,old,true) } =>* { admitted(undo,main,repaired) CFG:Cfg } .
search [1] in LANDING-GATE : { request(directStore,main,good,true,true) commit(directStore,main,good) current(old,old,old,old,old,old,old,true) } =>* { admitted(directStore,main,good) CFG:Cfg } .
search [1] in LANDING-GATE : { request(importDoor,main,good,true,true) commit(importDoor,main,good) current(old,old,old,old,old,old,old,true) } =>* { admitted(importDoor,main,good) CFG:Cfg } .
search [1] in LANDING-GATE : { request(retry,main,good,true,true) commit(retry,main,good) current(old,old,old,old,old,old,old,true) } =>* { admitted(retry,main,good) CFG:Cfg } .
search [1] in LANDING-GATE : { request(transport,release,good,true,true) commit(transport,release,good) current(old,old,old,old,old,old,old,true) } =>* { admitted(transport,release,good) CFG:Cfg } .
search [1] in LANDING-GATE : { request(undo,work,broken,false,false) commit(undo,work,broken) } =>* { ungated(undo,work,broken) CFG:Cfg } .
search [1] in LANDING-GATE : { request(transport,work,broken,false,false) commit(transport,work,broken) } =>* { ungated(transport,work,broken) CFG:Cfg } .
search [1] in LANDING-GATE : { bind(work) } =>* { bound(work) CFG:Cfg } .
search in LANDING-GATE : { certificate(promote,main,good,old,old,old,old,old,old,old) commit(promote,main,good) current(new,old,old,old,old,old,old,true) } =>* { admitted(promote,main,good) CFG:Cfg } .
search [1] in LANDING-GATE-WEAK-HEAD : { certificate(promote,main,good,old,old,old,old,old,old,old) commit(promote,main,good) current(new,old,old,old,old,old,old,true) } =>* { admitted(promote,main,good) CFG:Cfg } .
search in LANDING-GATE : { certificate(promote,main,good,old,old,old,old,old,old,old) commit(promote,main,good) current(old,new,old,old,old,old,old,true) } =>* { admitted(promote,main,good) CFG:Cfg } .
search [1] in LANDING-GATE-WEAK-CHARTER : { certificate(promote,main,good,old,old,old,old,old,old,old) commit(promote,main,good) current(old,new,old,old,old,old,old,true) } =>* { admitted(promote,main,good) CFG:Cfg } .
search in LANDING-GATE : { certificate(promote,main,good,old,old,old,old,old,old,old) commit(promote,main,good) current(old,old,new,old,old,old,old,true) } =>* { admitted(promote,main,good) CFG:Cfg } .
search [1] in LANDING-GATE-WEAK-EVIDENCE : { certificate(promote,main,good,old,old,old,old,old,old,old) commit(promote,main,good) current(old,old,new,old,old,old,old,true) } =>* { admitted(promote,main,good) CFG:Cfg } .
search in LANDING-GATE : { certificate(promote,main,good,old,old,old,old,old,old,old) commit(promote,main,good) current(old,old,old,new,old,old,old,true) } =>* { admitted(promote,main,good) CFG:Cfg } .
search [1] in LANDING-GATE-WEAK-AUTHORITY : { certificate(promote,main,good,old,old,old,old,old,old,old) commit(promote,main,good) current(old,old,old,new,old,old,old,true) } =>* { admitted(promote,main,good) CFG:Cfg } .
search in LANDING-GATE : { certificate(promote,main,good,old,old,old,old,old,old,old) commit(promote,main,good) current(old,old,old,old,new,old,old,true) } =>* { admitted(promote,main,good) CFG:Cfg } .
search [1] in LANDING-GATE-WEAK-INVENTORY : { certificate(promote,main,good,old,old,old,old,old,old,old) commit(promote,main,good) current(old,old,old,old,new,old,old,true) } =>* { admitted(promote,main,good) CFG:Cfg } .
search in LANDING-GATE : { certificate(promote,main,good,old,old,old,old,old,old,old) commit(promote,main,good) current(old,old,old,old,old,new,old,true) } =>* { admitted(promote,main,good) CFG:Cfg } .
search [1] in LANDING-GATE-WEAK-SELECTION : { certificate(promote,main,good,old,old,old,old,old,old,old) commit(promote,main,good) current(old,old,old,old,old,new,old,true) } =>* { admitted(promote,main,good) CFG:Cfg } .
search in LANDING-GATE : { certificate(promote,main,good,old,old,old,old,old,old,old) commit(promote,main,good) current(old,old,old,old,old,old,new,true) } =>* { admitted(promote,main,good) CFG:Cfg } .
search [1] in LANDING-GATE-WEAK-FENCE : { certificate(promote,main,good,old,old,old,old,old,old,old) commit(promote,main,good) current(old,old,old,old,old,old,new,true) } =>* { admitted(promote,main,good) CFG:Cfg } .
search in LANDING-GATE : { certificate(promote,main,good,old,old,old,old,old,old,old) commit(promote,main,good) current(old,old,old,old,old,old,old,false) } =>* { admitted(promote,main,good) CFG:Cfg } .
search [1] in LANDING-GATE-WEAK-EXPIRY : { certificate(promote,main,good,old,old,old,old,old,old,old) commit(promote,main,good) current(old,old,old,old,old,old,old,false) } =>* { admitted(promote,main,good) CFG:Cfg } .
search in LANDING-GATE : { certificate(promote,main,good,old,old,old,old,old,old,old) commit(promote,main,broken) current(old,old,old,old,old,old,old,true) } =>* { admitted(promote,main,broken) CFG:Cfg } .
search [1] in LANDING-GATE-WEAK-CANDIDATE : { certificate(promote,main,good,old,old,old,old,old,old,old) commit(promote,main,broken) current(old,old,old,old,old,old,old,true) } =>* { admitted(promote,main,broken) CFG:Cfg } .
search in LANDING-GATE : { request(undo,main,broken,false,true) commit(undo,main,broken) current(old,old,old,old,old,old,old,true) } =>* { admitted(undo,main,broken) CFG:Cfg } .
search [1] in LANDING-GATE-WEAK-SUPPORT : { request(undo,main,broken,false,true) commit(undo,main,broken) current(old,old,old,old,old,old,old,true) } =>* { admitted(undo,main,broken) CFG:Cfg } .
search in LANDING-GATE : { request(promote,main,good,true,false) commit(promote,main,good) current(old,old,old,old,old,old,old,true) } =>* { admitted(promote,main,good) CFG:Cfg } .
search [1] in LANDING-GATE-WEAK-SELF-GRANT : { request(promote,main,good,true,false) commit(promote,main,good) current(old,old,old,old,old,old,old,true) } =>* { admitted(promote,main,good) CFG:Cfg } .
search in LANDING-GATE : { commit(directStore,main,broken) current(old,old,old,old,old,old,old,true) } =>* { admitted(directStore,main,broken) CFG:Cfg } .
search [1] in LANDING-GATE-WEAK-BYPASS : { commit(directStore,main,broken) current(old,old,old,old,old,old,old,true) } =>* { admitted(directStore,main,broken) CFG:Cfg } .
search in LANDING-GATE : { bind(main) } =>* { bound(main) CFG:Cfg } .
search [1] in LANDING-GATE-WEAK-BIND : { bind(main) } =>* { bound(main) CFG:Cfg } .
quit
